#include "main.h"

/**
 * Main - A function that prints alphabet x10
 * Return 0 (Success)
 */
void print_alphbet_x10(void)
{
	char (i, c);
	for (i = 10; i <=10, i++)
	{
        	for (c = 'a'; c <= 'z'; c++)
        	{
        	_putchar(i, c);
        	}
        	_putchar('\n');
	}
}
