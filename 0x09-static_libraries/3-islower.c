#include "main.h"

/**
 * _islower - function checks for lowercase character
 * @c: the character utilized to check letter
 * Return: 1 if c is lowercase, else return 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
