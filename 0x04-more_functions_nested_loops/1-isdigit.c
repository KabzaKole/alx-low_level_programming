#include "main.h"
/**
 * _isdidgit-checks for a digit (0 through 9)
 * @c:int to be checked
 * Return: 1 if cis a digit, otherwise  return 0
 */
int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
