#include "main.h"

/**
 * _isdigit - checks for a digit between 0 and 9
 * @c: integer to be assessed
 * Return: 1 if c is a digit, else return 0
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
